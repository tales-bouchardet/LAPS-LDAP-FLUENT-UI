# LAPS-LDAP-FLUENT-UI
Um exemplo simples de codigo em C# para consulta de senha de maquinas no AD, com algumas funcionalidades extras.

Montar diretamente não será funcional, é preciso adicionar um caminho LDAP com servidor e DC.

'dotnet publish -c Release -r win-x64 -o publish'
